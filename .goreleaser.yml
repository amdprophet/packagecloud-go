before:
  hooks:
    - go mod download
builds:
- binary: packagecloud
  main: ./cmd/packagecloud/main.go
  env:
  - CGO_ENABLED=0
checksum:
  name_template: 'checksums.txt'
snapshot:
  name_template: "{{ .Tag }}-next"
changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'
